<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EasyWorks"
        tools:targetApi="31">
        <activity
            android:name=".activity.dashboard1"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity android:name=".activity.Login"></activity>
        <activity android:name=".activity.Registration"></activity>
        <activity android:name=".activity.Profile"></activity>
        <activity android:name=".activity.UpdateLoginpassword"></activity>
        <activity android:name=".activity.SignupActivity"></activity>
        <activity android:name=".activity.Accountsettings"></activity>
        <activity android:name=".activity.Notification"></activity>
        <activity android:name=".activity.TestSingin"></activity>
        <activity android:name=".activity.FetchingActivity"></activity>



    </application>

</manifest>